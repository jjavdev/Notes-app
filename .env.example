# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=notes_app

# Backend port
PORT=3001

# JWT secret
JWT_SECRET=notes-app-secret-key-2024

# Full connection string (overrides individual DB_* vars if set)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/notes_app
