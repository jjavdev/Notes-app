{
  "name": "notes-app",
  "private": true,
  "engines": { "node": "^22.16.0", "npm": "^10.0.0" },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "node backend/dist/main.js"
  }
}